package app.banco.apiBanco.record;

public record RecordEndereco(String logradouro,
                             String numero,
                             String bairro,
                             String cep,
                             String complemento) {
}
